<ul>
<% @jobs.each do |job| %>
    <li>
      <h3><%= job.name %> (<%= job.status.to_s%>)</h3>
      <ul>
        <% job.builds.each do |build| %>
        <li><%= build.name %> (<%= build.success? ? 'worked' : 'failed' %>)</li>
        <% end %>
      </ul>
    </li>
<% end %>
</ul>